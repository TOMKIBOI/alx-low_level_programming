#include <stdio.h>

/**
 * main - print base 16
 *
 *Return: Always 0 (success)
 */

int main(void)
{
	int a;
	char d;

	for (a = 0; a < 10; a++)
		putchar((i % 10) + '0');
	for (d = 97; d <= 102; d++)
		putchar(d);
	putchar('\n');
	return (0);
}
