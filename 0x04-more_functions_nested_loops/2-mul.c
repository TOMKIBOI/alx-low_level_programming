#include "main.h"
#include <stdio.h>

/**
 *mul - multiplies 2 numbers
 *
 *@a: argument to mul
 *@b: argument to mul
 *
 *Return: multiplaction of a and b (success)
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
